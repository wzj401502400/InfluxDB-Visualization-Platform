apiVersion: 1

datasources:
  - name: InfluxDB-Flux
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: devorg
      defaultBucket: devbucket
    secureJsonData:
      token: devtoken
    isDefault: true
    editable: true
